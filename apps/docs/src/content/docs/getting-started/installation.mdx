---
title: Installation
description: Install and set up Xtarterize.
---

# Installation

## Prerequisites

- Node.js 18 or later
- A JavaScript/TypeScript project (or starting fresh)

## Run Without Installing

You can use Xtarterize without installing it globally by using `npx`:

```bash
npx xtarterize init
```

This is the recommended approach — you always get the latest version.

## Verify It Works

```bash
npx xtarterize --version
npx xtarterize --help
```

## Next Steps

Now that Xtarterize is ready, you can:

1. [Initialize a project](/getting-started/initialization/) — run `init` to detect and apply conformance
2. [Check status](/guide/cli/overview/) — run `check` to audit current conformance
3. [Explore tasks](/guide/tasks/overview/) — see what conformance tasks are available
